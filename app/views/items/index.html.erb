<h1>View Items Available</h1>

 <% @items.each do |item|%>
   <p>
      <%= link_to item.name, item_path(item) %> - :price
      <%= item.price %> -
      description: <%= item.description %> 
              
    </p>
    
  <% end %>

  <%=link_to "See updated list", new_list_item_path(@list_id) %> 
  