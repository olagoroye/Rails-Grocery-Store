<h1>View Items Available</h1>

  <% @items.each do |item|%>
    <li>
      <%= link_to item.name, list_item_path(items) %> - :price
      <%= item.price %> -
      description: <%= item.description %> 
        <ul>
         
    </li>
  <% end %>
